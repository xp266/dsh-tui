export const DIALOG_WIDTH_MEDIUM = 60
export const DIALOG_WIDTH_LARGE = 88

export const DIALOG_MAX_HEIGHT = 0.6

/**
 * Every row of the window counts toward this floor — padding, title, footer,
 * and blank background alike. Held like the width clamp: only squeezed when
 * the terminal itself cannot fit it.
 */
export const DIALOG_MIN_HEIGHT = 12

export const INPUT_MAX_ROWS = 3
export const ERROR_MAX_ROWS = 3
