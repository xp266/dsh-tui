diff --git a/build/cursor-helpers.js b/build/cursor-helpers.js
index be493545f0a22d2972a36ad858d2e4bbc4c9326a..8669da72bb33f56f46decc99424c59ffb26522ce 100644
--- a/build/cursor-helpers.js
+++ b/build/cursor-helpers.js
@@ -38,9 +38,8 @@ Hides cursor if it was previously shown, returns to bottom, then repositions.
 */
 export const buildCursorOnlySequence = (input) => {
     const hidePrefix = input.cursorWasShown ? hideCursorEscape : '';
-    const returnToBottom = buildReturnToBottom(input.previousLineCount, input.previousCursorPosition);
-    const cursorSuffix = buildCursorSuffix(input.visibleLineCount, input.cursorPosition);
-    return hidePrefix + returnToBottom + cursorSuffix;
+    // absolute move: relative round-trip drifts -1 row for frames without trailing newline
+    return hidePrefix + `\x1b[${input.cursorPosition.y};${input.cursorPosition.x + 1}H` + showCursorEscape;
 };
 /**
 Build the prefix that hides cursor and returns to bottom before erasing or rewriting.
